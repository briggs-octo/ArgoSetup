# Managed by Octopus

Do not edit by hand. Files in this directory are generated from `templates/`
by the "Update Argo CD Application Manifests" step and overwritten on every
deployment.

This placeholder exists so Argo CD can resolve the application path before the
first deployment. Argo CD's directory source only reads `.yaml`, `.yml` and
`.json`, so this file is ignored as a manifest. The step's purge option removes
it on first run.
